# Location-Based Service Search Chatbot Requirements
# Core dependencies for the chatbot system

# Required for OpenStreetMap integration
requests>=2.28.0      # For API calls to OpenStreetMap
geopy>=2.2.0          # For geocoding and distance calculations
overpy>=0.6           # For OpenStreetMap Overpass API queries

# Required for WhatsApp integration
flask>=2.0.0          # Web framework for webhook handling
flask-cors>=4.0.0     # Cross-Origin Resource Sharing for webhooks
python-dotenv>=0.19.0 # Environment variable management
# requests>=2.28.0    # Already included above for OSM integration

# Optional for different integrations (future use)
# python-telegram-bot>=20.0  # For Telegram integration
